type Query {
  recommendationSection(id: ID!, playlistLimit: Int): RecommendationSection
  recommendationSections(playlistLimit: Int): [RecommendationSection!]!
}

type RecommendationSection {
  id: ID!
  title: String!
  description: String
  playlists: [Playlist!]!
}
