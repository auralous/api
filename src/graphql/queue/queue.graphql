type QueueItem {
  trackId: String!
  creatorId: String!
}

type Queue {
  id: ID!
  items: [QueueItem!]!
}

type Query {
  queue(id:ID!): Queue
}

type Mutation {
  queueAdd(id: ID!, tracks: [ID!]!): Boolean!
  queueRemove(id: ID!, trackId: ID!, creatorId: ID!): Boolean!
  queueReorder(id: ID!, position: Int!, insertPosition: Int!): Boolean!
}

type Subscription {
  queueUpdated(id: ID!): Queue!
}
